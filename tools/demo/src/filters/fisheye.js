export default function() {
    const app = this;
    app.addFilter('AmoyFishEyeFilter', {
        enabled: false,
        global: false,
        opened: false,
        oncreate(folder) {
         
        }
    });
}
