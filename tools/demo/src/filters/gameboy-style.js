export default function() {
    const app = this;
    app.addFilter('AmoyGameboyStyleFilter', {
        enabled: false,
        global: false,
        opened: false,
        oncreate(folder) {

        }
    });
}
