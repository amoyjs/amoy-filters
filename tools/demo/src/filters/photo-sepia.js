export default function() {
    const app = this;
    app.addFilter('AmoyPhotoSepiaToningFilter', {
        enabled: false,
        global: false,
        opened: false,
        oncreate(folder) {

        }
    });
}
